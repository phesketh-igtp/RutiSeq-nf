name: tbprofiler_env
channels:
  - defaults #needed defaults channele or the environment will not create
  - bioconda
  - conda-forge #needed defaults channele or the environment will not create
dependencies:
  - conda-forge::python=3.10
  - bioconda::tb-profiler=6.6.3