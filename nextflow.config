// Enable DSL2
nextflow.enable.dsl = 2

// Create timestamp definition
def timestamp = new java.util.Date().format("yyyy-MM-dd_HH-mm-ss")

params {
    // Output directory
        outDir                  = null
        runID                   = null
        workDir                 = null
        envsDir                 = "${params.outDir}/envs/"
        cpus                    = 4 //  default thread values.

    // Database path
        sylph_bbdd              = "/imppc/labs/emlab/share/bbdd/sylph/gtdb-r220-c200-dbv1.syldb /imppc/labs/emlab/share/bbdd/sylph/fungi-refseq-2024-07-25-c200-v0.3.syldb /imppc/labs/emlab/share/bbdd/sylph/imgvr_c200_v0.3.0.syldb"
        sylph_bbdd_id           = "GTDB_r220 IMGVR_4.1 FungiRefSeq-2024-07-25"
        tbprofiler_tbdb         = "${projectDir}/db/tb-profiler_db/tbdb"
        tbprofiler_who          = "${projectDir}/db/tb-profiler_db/who"
        mtbseq_gene_annotations = "${projectDir}/db/H37Rv_db/M._tuberculosis_H37Rv_2015-11-13_genes.txt"
        r_script_dir            = "${projectDir}/bin/R"
        script_dir              = "${projectDir}/bin"
        mtbc_ancestor_path      = "${projectDir}/db/MTBc_ancestral_sequence/MTB_ancestor_reference.pos.gz"
        snpeff_ref_genome       = "Mycobacterium_tuberculosis_h37rv"
        mtbc_reference          = "${projectDir}/db/H37Rv_db/M._tuberculosis_H37Rv_2015-11-13.fasta"
        // this bbdd will be populated with SNP barcodes of YOUR clusters BUT only if they are generated through the workflow
        rutiseq_db              = "${projectDir}/db/RutiSeq-db"

    // Lineages for clustering (this is defined by the TB-Profiler lineage classification)
    /// The purpose of this is to break up the genomes into smaller more computationally manageble chunks for analysis
    /// the lists will depend entirely on what the bulk of your data is, in this case it is presumed that lineage 4
    /// will be the most abundant, thus we take a level 2 sub-lineage classification to perform the clustering at
    /// but not for other lineages as they all have <800 genomes. Once a lineage had around that amount, you want to take the 
    /// next sub-lineage level (given as a tuple).

        pairwise_split          = "none" // "none", "main", or "sub"

        lineage_pairwise_main   = [ "La1", "La2", "La3", "lineage1", "lineage2", "lineage3", 
                                    "lineage4", "lineage5", "lineage6" ]

        lineage_pairwise_sub    = [ "lineage4.1", "lineage4.2", "lineage4.3", "lineage4.4", 
                                    "lineage4.5", "lineage4.6", "lineage4.7", "lineage4.8", 
                                    "lineage4.9" ]

    // fastp parameters
        fastp_length_required   = 50 // minimum number of reads required for analysis
        fastp_max_reads         = 5000000 // maximum number of reads to be used in the analysis
        fastp_min_reads         = 500000 // minimum number of reads required for analysis

    // MTBseq default values
        mtbseq_minbqual         = 20    // default: 13
        mtbseq_mincovf          = 4     // default: 4
        mtbseq_mincovr          = 4     // default: 4
        mtbseq_minphred20       = 4     // default: 4
        mtbseq_minfreq          = 75    // default: 75
        mtbseq_unambig          = 95    // default: 95
        mtbseq_window           = 12    // default: 12
        mtbseq_snp_distance     = ["5", "10", "15"] // a tuple of all the distances to compared; default: 12

    // IQ-Tree bootstraps
        iqtree_bootstraps       = 1000
        iqtree_model            = 'GTR+G4'

    // Genome filtering parameters for pairwise analysis
        filt_min_cov            = 0.95 // minimum breadth coverage as calculated by MTBseq for pairwise analysis
        filt_min_depth          = 50 // minimum depth coverage as calculated by MTBseq for pairwise analysis
        filt_min_reads          = 1000000 // minimum number of reads required for MTBseq analysis
    
    // ONT parameters (WIP)
        ont_filt_min_cov        = 0.95 // minimum breadth coverage as calculated by MTBseq for pairwise analysis
        ont_filt_min_depth      = 30 // minimum depth coverage as calculated by MTBseq for pairwise analysis
        ont_filt_min_reads      = 50000

    // Conda paths (built from yml files):
        envs_dir                    = "${projectDir}/envs/conda"
        mtbseq_env                  = "${projectDir}/envs/conda/mtbseq_env.yml"
        phylogeny_env               = "${projectDir}/envs/conda/phylogeny_env.yml"
        r_phylogeny_env             = "${projectDir}/envs/conda/r-phylogeny_env.yml"
        r_stats_env                 = "${projectDir}/envs/conda/r-stats_env.yml"
        snp_profiling_env           = "${projectDir}/envs/conda/snp_profiling_env.yml"
        taxonomy_env                = "${projectDir}/envs/conda/taxonomy_env.yml"
        tbprofiler_env              = "${projectDir}/envs/conda/tbprofiler_env.yml"
        
    // Docker images (from seqera container builder):
        docker_mtbseq_img           = "community.wave.seqera.io/library/mtbseq_openjdk_python:4ea995ae57fb2cd9" //https://wave.seqera.io/view/builds/bd-4ea995ae57fb2cd9_1
        docker_phylogeny_img        = "community.wave.seqera.io/library/iqtree_mafft_seqkit_snp-sites_treetime:030632716d453b3f" //https://wave.seqera.io/view/builds/bd-030632716d453b3f_1
        docker_r_phylogeny_img      = "community.wave.seqera.io/library/bioconductor-biostrings_bioconductor-ggtree_bioconductor-treeio_r-ape_pruned:8a8ce944c25bf5fc" //https://wave.seqera.io/view/builds/bd-8a8ce944c25bf5fc_1
        docker_r_stats_img          = "community.wave.seqera.io/library/r-argparse_r-base_r-dplyr_r-ggrepel_pruned:9fa710c1508e1cb6" //https://wave.seqera.io/view/builds/bd-9fa710c1508e1cb6_1
        docker_snp_profiling_img    = "community.wave.seqera.io/library/bcftools_emboss_seqkit_snp-sites_pruned:1a1022abcb91db63" //https://wave.seqera.io/view/builds/bd-1a1022abcb91db63_1
        docker_taxonomy_img         = "" //https://wave.seqera.io/view/builds/bd-53a1ea914b4e7099_1
        docker_tbprofiler_img       = "community.wave.seqera.io/library/bcftools_tb-profiler:047e29be90617326" //https://wave.seqera.io/view/builds/bd-047e29be90617326_2

    // Apptainer images
        apptainer_mtbseq_img        = "https://community-cr-prod.seqera.io/docker/registry/v2/blobs/sha256/3a/3ad60ca6d984c352a6ae57fc4f3fb9328bfd75afe08f701d6f809a087de92eed/data" //https://wave.seqera.io/view/builds/bd-f24ae45236bbfe30_1
        apptainer_phylogeny_img     = "https://community-cr-prod.seqera.io/docker/registry/v2/blobs/sha256/19/1969316c878dcf7c6e1cc6a5ccf9da86409a6effa02b8c84b7214f0aff6d8922/data" //https://wave.seqera.io/view/builds/bd-1ec44de5738a92e4_1
        apptainer_r_phylogeny_img   = "https://community-cr-prod.seqera.io/docker/registry/v2/blobs/sha256/7d/7d61844e2ab6d8b4820ce74fc28272afd98c4c0613700f4e243487070c51fc34/data" //https://wave.seqera.io/view/builds/bd-3341c4aaf45c18b8_1
        apptainer_r_stats_img       = "https://community-cr-prod.seqera.io/docker/registry/v2/blobs/sha256/1d/1d281274b3b9cace936bea889028c0b9713ed7cb2c2c7781379c41006b866bc3/data" //https://wave.seqera.io/view/builds/bd-4a792c8d82b891d0_1
        apptainer_snp_profiling_img = "https://community-cr-prod.seqera.io/docker/registry/v2/blobs/sha256/db/db3337ec6f003d7a5f87d110704d2fa8d0440cf2c86fdae86317ed956a87eb74/data" //https://wave.seqera.io/view/builds/bd-8fe1733ce51eacc9_1
        apptainer_taxonomy_img      = "https://community-cr-prod.seqera.io/docker/registry/v2/blobs/sha256/23/23830e0738090f67fff2bb075d2b1c22399e9fa15790b0df6525d4f072e4852b/data" //https://wave.seqera.io/view/builds/bd-53a1ea914b4e7099_1
        apptainer_tbprofiler_img    = "https://community-cr-prod.seqera.io/docker/registry/v2/blobs/sha256/1c/1c8630f93dc3f64b11a2ff508d950a3e06c4e33e949476ed2b475beb8cafd1c9/data" //https://wave.seqera.io/view/builds/bd-400be6f998a9ed1b_1

    // Error strategy
        process.errorStrategy = 'finish'
    }

/* 
    PROFILES:
        Personal profile for running on the IGTP computing cluster
        nextflow run main.nf [···] -profile igtp,singularity_on -ansi-log false
*/

profiles {
    conda_on {
            conda.enabled           = true
            conda.useMamba          = true
            conda.createTimeout     = '2 h'
            conda.cacheDir          = "${params.envsDir}/nf-conda/"
            conda.createOptions     = "-y" // --no-shortcuts
    }

    apptainer_on {
            apptainer.enabled       = true
            apptainer.autoMounts    = true
            apptainer.cacheDir      = "${params.envsDir}/nf-apptainer/"
    }

    singularity_on {
            singularity.enabled     = true
            singularity.autoMounts  = true
            singularity.cacheDir    = "${params.envsDir}/nf-singularity/"
    }

    SGE {
        process {
            executor            = 'sge'
            penv                = 'smp'
            clusterOptions      = '-S /bin/bash' // '-V' solve a singularity issue ???
            beforeScript        = 'source /imppc/labs/emlab/phesketh/miniconda3/etc/profile.d/conda.sh'

        /*
            MODULE RESOURCE ALLOCATIONS AND DEFINING ADDITIONAL ARGUMENTS
        */

            withName: SYLPH_CLASSIFICATION {
                cpus                        = { Math.min( 8 * task.attempt, 12 ) }
                memory                      = 48.GB
                time                        = 1.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'ignore' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                }

            /*
                Workflow: SINGKE_WF()
            */

            withName: FILE_CHECK {
                cpus                        = 1
                memory                      = 6.Gb
                time                        = 5.min
                }

            withName: ADAPTORS_AND_DOWNSAMPLING {
                cpus                        = { Math.min( 1 + task.attempt, 28 ) }
                memory                      = { 6.GB * task.attempt }
                time                        = 1.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                }

            withName: TBPROFILER_PROFILE_TBDB {
                cpus                        = { Math.min( 1 + task.attempt, 28 ) }
                memory                      = { 16.GB * task.attempt }
                time                        = 1.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                ext.additional_args         = '' // tb-profiler args
                }

            withName: TBPROFILER_PROFILE_WHO {
                cpus                        = { Math.min( 1 + task.attempt, 28 ) }
                memory                      = { 16.GB * task.attempt }
                time                        = 1.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                ext.additional_args = '' // tb-profiler args
                }

            withName: MTBSEQ_SINGLE {
                cpus                        = { Math.min( 3 + task.attempt, 28 ) }
                memory                      = { 16.GB * task.attempt }
                time                        = 1.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                // MTBseq args - if have to ensure that ALL MTBSEQ related additional arguments are the same
                ext.additional_args         = ''
                }

            withName: SNP_PROFILING_SINGLE {
                cpus                        = { Math.min( 1 + task.attempt, 28 ) }
                memory                      = { 6.GB * task.attempt }
                time                        = 1.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                ext.additional_args_varscan = '--min-coverage 20 --min-reads2 20 --min-avg-qual 20' // varscan args
                }

            withName: SNP_ANNOTATING_SINGLE {
                cpus                        = { Math.min( 1 + task.attempt, 28 ) }
                memory                      = { 6.GB * task.attempt }
                time                        = 1.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                ext.additional_str_snpeff   = '-v -hgvs1LetterAa -noStats -no-downstream -no-intergenic -no-intron -no-upstream -noLof '
                //ext.additional_args_filt_snps = ['PE/PPE/PGRS', 'maturase', 'phage', 'mobile','13E12 repeat family protein']
                }

            /*
                Workflow: PAIRWISE_WF()
            */

            withName: TBPROFILER_COMPILE_TBDB {
                cpus                        = { Math.min( 1 + task.attempt, 28 ) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                ext.additional_args         = '' // tb-profiler args
                }
            
            withName: TBPROFILER_COMPILE_WHO {
                cpus                        = { Math.min( 1 + task.attempt, 28 ) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                ext.additional_args = '' // tb-profiler args
                }

            withName: MTBSEQ_STATS_COMPILE {
                cpus                        = { Math.min(1 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                ext.additional_args         = '' // tb-profiler args
                }

            withName: COMPILE_SEQUENCING_STATS {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                ext.additional_args = '' // tb-profiler args
                }

            withName: MTBSEQ_LINEAGE_JOINT_AMEND {
                cpus                        = { Math.min(7 + task.attempt, 28) }
                memory                      = 32.GB
                time                        = 24.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                                            // MTBseq args - if have to ensure that ALL MTBSEQ related additional arguments are the same
                ext.additional_args         = ''
                }

            withName: MTBSEQ_LINEAGE_GROUP {
                cpus                        = { Math.min(3 + task.attempt, 28) }
                memory                      = { 16.GB * task.attempt }
                time                        = 24.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                // MTBseq args - if have to ensure that ALL MTBSEQ related additional arguments are the same
                ext.additional_args         = ''
                }

            withName: SNP_PHYLOGENY {
                cpus                        = { Math.min(6 + task.attempt, 28) }
                memory                      = { 16.GB * task.attempt }
                time                        = 12.hour
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                // Customised the running parameters by adding flag below:
                ext.additional_args         = '' // tb-profiler args
                }

            withName: CONCATENATE_CLUSTERS {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 6.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                }

            /*
                Workflow: SUMMARY_WF()
            */

            withName: PLOT_MAIN_PHYLOGENY {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                }

            withName: GENERATE_SUMMARY_REPORT {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'finish' }
                maxRetries                  = 3
                }

            withName: PREPARE_NEXUS_PATHS {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'ignore' }
                maxRetries                  = 3
                }

            withName: GENERATE_NEXUS {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'ignore' }
                maxRetries                  = 3
                }

            withName: TABULATE_VARIANT_SITES {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'ignore' }
                maxRetries                  = 3
                }

            withName: CONCATENATED_VARIANT_FILES {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.attempt <= 3 ? 'retry' : 'ignore' }
                maxRetries                  = 3
                }

            withName: GENERATE_TIMETREES {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.exitStatus in 137..140 ? 'retry' : 'ignore' }
                maxRetries                  = 3
                }

            withName: PLOT_TIMETREES {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.attempt <= 3 ? 'retry' : 'ignore' }
                maxRetries                  = 3
                }

            withName: GENERATE_NEXUS_W_MRCA {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.attempt <= 3 ? 'retry' : 'ignore' }
                maxRetries                  = 3
                }

            withName: DATA_DELIVERY {
                cpus                        = { Math.min(2 + task.attempt, 28) }
                memory                      = { 8.GB * task.attempt }
                time                        = 30.min
                errorStrategy               = { task.attempt <= 3 ? 'retry' : 'ignore' }
                maxRetries                  = 3
                }
            }
        }
    }

/*
    LOGGING:
*/

    report {
        enabled = true
        file = "${params.outDir}/pipeline_info/execution_report_${timestamp}.html"
        showSkipped = false
        showTaskCacheInfo = false
        showTaskResources = true
        showTaskResourcesPercentage = true
    }

    timeline {
        enabled     = true
        file        = "${params.outDir}/pipeline_info/execution_timeline_${timestamp}_${params.runID}.html"
    }

    trace {
        enabled     = true
        file        = "${params.outDir}/pipeline_info/execution_trace_${timestamp}_${params.runID}.tsv"
        fields      = 'task_id,hash,native_id,process,tag,name,status,exit,module,container,cpus,time,disk,memory,attempt,submit,start,complete,duration,realtime,queue,%cpu,%mem,rss,vmem,peak_rss,peak_vmem,rchar,wchar,syscr,syscw,read_bytes,write_bytes'
    }

    dag {
        enabled     = true
        file        = "${params.outDir}/pipeline_info/pipeline_dag_${timestamp}_${params.runID}.png"
        file        = "${params.outDir}/pipeline_info/pipeline_dag_${timestamp}_${params.runID}.mmd"
        direction   = 'TB'
    }

    manifest {
        name        = 'RutiSeq-nf'
        author      = 'Poppy J. Hesketh Best'
        homePage    = 'https://github.com/phesketh-igtp/TBSEQ.cat-nf'
        description = 'Mapping, variant calling and genomic clustering for the ongoing monitoring of MTB outbreaks in Catalunya'
        mainScript  = 'main.nf'
        nextflowVersion = '24.10.2'
        version     = '1.0.0 [pre_release]'
    }